import React from "react";

export const metadata = {
  title: "Kapcsolat - Ultramarinos Martínez",
};

const Pagelayout = ({ children }) => {
  return <>{children}</>;
};

export default Pagelayout;
